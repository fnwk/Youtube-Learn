import { Text, View } from "react-native";

const LoginScreen = () => {
  console.log("LoginScreen");
  return (
    <View className="bg-dark">
      <Text className="color-white bg-dark">Login Screen</Text>
    </View>
  );
};

export default LoginScreen;
